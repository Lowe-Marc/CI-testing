name: Testing
on:
  push:
    branches:
      - qa
      - master
      - testing
  repository_dispatch:
    types: [deployment-testing]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "Testing"
